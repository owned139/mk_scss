# default configuration
plugin.tx_mkscss {
    settings {
        ## enable sourcemaps
        sourcemaps = 1
        ## Possible values are file and inline
        sourcemapType = file
        ## css output formatting: Expanded, Compressed
        cssFormatter = Expanded
    }
}